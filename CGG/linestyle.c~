

#include <graphics.h>
 
int main()
{
   int gd = DETECT, gm, x = 100, y = 50;
 
   initgraph(&gd, &gm, NULL);
 
   setlinestyle(4,1,3);
   line(x, y, x+200, y);
   y = y + 25;
    //setlinestyle(0,1,3);
// line(x, y, x+400, y);
  // y = y + 100;
   getch();
   closegraph();
   return 0;
}


